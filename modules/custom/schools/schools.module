<?php

/**
 * Implements hook_menu().
 */
function schools_menu() {
/**
 * Sample code from the api example.
 *  items['blog'] = array(
 *    'title' => 'blogs',
 *    'page callback' => 'blog_page',
 *    'access arguments' => array('access content'),
 *    'type' => MENU_SUGGESTED_ITEM,
 *);
 *return items;
 */

 /* Your code here */
 $items = array();
 $items['schools'] = array(
   'title' => 'Schools Overview',
   'description' => 'Schools overview page',
   'page callback' => 'schools_overview',
   'access callback' => TRUE
 );

 return $items;

}

/**

 * Implements hook_init().

 */

function schools_init() {
  // Prevent the page from being cached.
  // drupal_page_is_cacheable(FALSE);
  // drupal_flush_all_caches();
}

function schools_overview() {
  // $list = theme('item_list', array(
  //   'items' => array(
  //     'item A',
  //     'item B',
  //     'item C',
  //   ),
  //   'title' => 'List of Schools',
  // )); // theme_item_list

  $list = array(
    '#theme' => 'item_list',
    '#items' => array(
      'item A',
      'item B',
      'item C',
      'item D',
    ),
    '#title' => 'List of Schools',
  );

  dpm($list);
  
  // option 1: render the list as markup
  // return array(
  //   '#markup' =>  $list,
  // );

  // option 2: use the theme directly in the render array
  return $list;
}
