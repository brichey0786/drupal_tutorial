<?php

/**
 * Implements hook_menu().
 */
function schools_menu() {
/**
 * Sample code from the api example.
 *  items['blog'] = array(
 *    'title' => 'blogs',
 *    'page callback' => 'blog_page',
 *    'access arguments' => array('access content'),
 *    'type' => MENU_SUGGESTED_ITEM,
 *);
 *return items;
 */

 /* Your code here */
 $items = array();
 $items['schools'] = array(
   'title' => 'Schools Overview',
   'page callback' => 'schools_overview_page',
   'access callback' => TRUE
 );
 $items['schools/%'] = array(
  'title' => 'Schools Form',
  'page callback' => 'schools_form_page',
  'access callback' => TRUE
);

 return $items;

}

/**

 * Implements hook_init().

 */

function schools_init() {
  // Prevent the page from being cached.
  // drupal_page_is_cacheable(FALSE);
  // drupal_flush_all_caches();
}

function schools_overview_page() {

  // option 1: use anchor tag directly
  // $link = '<a href="/schools/1">Link 1</a>';

  // option 2: theme_link()
  // $link = theme('link', array(
  //   'text' => 'Link 2',
  //   'path' => '/schools/1',
  //   'options' => array(
  //     'attributes' => array(),
  //     'html' => FALSE,
  //   )
  // ));

  $link = array(
    '#theme' => 'link',
    '#text' => 'Link 3',
    '#path' => '/schools/1',
    '#options' => array(
      'attributes' => array(),
      'html' => FALSE,
    ),
  );

  // dpm($link);

  // $link_rendered = drupal_render($link);

  // dpm($link_rendered);

  // option 3: l()
  // $link = l(t('Link 4'), '/schools/1', array('attributes' => array()));

  return $link;

}


function schools_form_page() {
  return array(
    '#markup' => 'schools_form_page',
  );
}